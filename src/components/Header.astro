---
/**
 * src/components/Header.astro
 * Sticky navigation bar with desktop links, mobile slide-out menu,
 * and scroll-aware shadow. Active link highlighted via pathname match.
 */

const pathname = Astro.url.pathname;

const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/about/', label: 'About' },
  { href: '/products/', label: 'Products' },
  { href: '/clients/', label: 'Clients' },
  { href: '/contact/', label: 'Contact' },
];

function isActive(href: string): boolean {
  if (href === '/') return pathname === '/';
  return pathname.startsWith(href);
}
---

<header
  id="site-header"
  class="fixed top-0 left-0 w-full z-50 bg-white/90 backdrop-blur-lg border-b border-transparent transition-all duration-300"
  role="banner"
>
  <div class="container-main section-padding">
    <div class="flex items-center justify-between h-20 lg:h-[72px]">

      <!-- ━━━ Logo ━━━ -->
<a href="/" class="flex items-center gap-2.5 group" aria-label="Reve Stitching — Home">
  <img 
    src="/images/icon.png" 
    alt="" 
    class="w-12 h-12 rounded-lg transition-transform duration-300 group-hover:scale-105"
  />
  <div class="leading-none">
    <span class="font-serif font-bold text-xl text-zinc-900">Reve</span>
    <span class="font-serif font-bold text-xl text-primary ml-0.5">Stitching</span>
  </div>
</a>

      <!-- ━━━ Desktop Navigation ━━━ -->
      <nav class="hidden lg:flex items-center gap-1" aria-label="Main navigation">
        {navLinks.map((link) => (
          <a
            href={link.href}
            class:list={[
              'relative px-4 py-2 text-[13px] font-semibold tracking-wide uppercase rounded-md transition-colors duration-200',
              isActive(link.href)
                ? 'text-primary bg-surface'
                : 'text-zinc-600 hover:text-primary hover:bg-surface/60',
            ]}
            aria-current={isActive(link.href) ? 'page' : undefined}
          >
            {link.label}
          </a>
        ))}
      </nav>

      <!-- ━━━ Desktop CTA + Mobile Toggle ━━━ -->
      <div class="flex items-center gap-3">
        <a
          href="/contact/"
          class="hidden lg:inline-flex btn-primary !py-3 !px-6 !text-xs"
          data-magnetic
        >
          Get a Quote
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12h14M12 5l7 7-7 7"/>
          </svg>
        </a>

        <!-- Hamburger toggle (mobile) -->
        <button
          id="mobile-menu-toggle"
          class="lg:hidden relative w-11 h-11 flex items-center justify-center rounded-lg hover:bg-zinc-100 transition-colors"
          aria-label="Open menu"
          aria-expanded="false"
          aria-controls="mobile-menu"
        >
          <div id="hamburger-icon" class="w-5 h-4 flex flex-col justify-between transition-all duration-300">
            <span class="block h-0.5 w-full bg-zinc-800 rounded transition-all duration-300 origin-center" id="bar1"></span>
            <span class="block h-0.5 w-full bg-zinc-800 rounded transition-all duration-300" id="bar2"></span>
            <span class="block h-0.5 w-3.5 bg-zinc-800 rounded transition-all duration-300 origin-center" id="bar3"></span>
          </div>
        </button>
      </div>
    </div>
  </div>

  <!-- ━━━ Mobile Menu Overlay ━━━ -->
  <div
    id="mobile-menu"
    class="fixed inset-0 top-20 bg-white z-40 lg:hidden translate-x-full transition-transform duration-500 ease-[cubic-bezier(0.22,1,0.36,1)]"
    aria-hidden="true"
  >
    <nav class="flex flex-col p-8 pt-6 gap-2" aria-label="Mobile navigation">
      {navLinks.map((link, i) => (
        <a
          href={link.href}
          class:list={[
            'text-2xl font-semibold py-3 border-b border-zinc-100 transition-colors duration-200',
            isActive(link.href) ? 'text-primary' : 'text-zinc-800 hover:text-primary',
          ]}
          style={`transition-delay: ${i * 60}ms`}
        >
          {link.label}
        </a>
      ))}
      <a href="/contact/" class="btn-primary mt-6 text-center">
        Get a Quote
      </a>
    </nav>
  </div>
</header>

<!-- Spacer so content isn't hidden behind fixed header -->
<div class="h-20 lg:h-[72px]" aria-hidden="true"></div>

<script>
  document.addEventListener('astro:page-load', () => {
    const header = document.getElementById('site-header');
    const toggle = document.getElementById('mobile-menu-toggle');
    const menu = document.getElementById('mobile-menu');
    const bar1 = document.getElementById('bar1');
    const bar2 = document.getElementById('bar2');
    const bar3 = document.getElementById('bar3');
    let isOpen = false;

    // Scroll shadow
    const handleScroll = () => {
      if (!header) return;
      if (window.scrollY > 20) {
        header.classList.add('shadow-lg', 'shadow-black/[0.04]', 'border-zinc-200/60');
      } else {
        header.classList.remove('shadow-lg', 'shadow-black/[0.04]', 'border-zinc-200/60');
      }
    };
    window.addEventListener('scroll', handleScroll, { passive: true });
    handleScroll();

    // Mobile menu toggle
    toggle?.addEventListener('click', () => {
      isOpen = !isOpen;
      toggle.setAttribute('aria-expanded', String(isOpen));
      menu?.setAttribute('aria-hidden', String(!isOpen));

      if (isOpen) {
        menu?.classList.remove('translate-x-full');
        menu?.classList.add('translate-x-0');
        bar1?.classList.add('rotate-45', 'translate-y-[7px]');
        bar2?.classList.add('opacity-0');
        bar3?.classList.add('-rotate-45', '-translate-y-[7px]', '!w-full');
        document.body.style.overflow = 'hidden';
      } else {
        menu?.classList.add('translate-x-full');
        menu?.classList.remove('translate-x-0');
        bar1?.classList.remove('rotate-45', 'translate-y-[7px]');
        bar2?.classList.remove('opacity-0');
        bar3?.classList.remove('-rotate-45', '-translate-y-[7px]', '!w-full');
        document.body.style.overflow = '';
      }
    });

    // Close menu on link click
    menu?.querySelectorAll('a').forEach((link) => {
      link.addEventListener('click', () => {
        if (isOpen) toggle?.click();
      });
    });
  });
</script>